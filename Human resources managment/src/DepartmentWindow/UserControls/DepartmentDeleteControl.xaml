<UserControl x:Class="Human_resources_managment.DepartmentWindow.UserControls.DepartmentDeleteControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Human_resources_managment.DepartmentWindow.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="20">
        <ComboBox x:Name="projBox"  IsEditable="True" IsTextSearchEnabled="False" 
        Text="{Binding FilterTextProject, UpdateSourceTrigger=PropertyChanged}" 
        SelectedItem="{Binding SelectedProj, Mode=TwoWay}" ItemsSource="{Binding FilteredProject}" 
        Width="200" Height="40" VerticalAlignment="Center" 
        Background="White" BorderThickness="1"
        Padding="5,0,5,0"
        FontSize="20"
        BorderBrush="Crimson"
        materialDesign:HintAssist.Hint="Отдел"
        materialDesign:ComboBoxAssist.ShowSelectedItem="True"
        Style="{StaticResource MaterialDesignOutlinedComboBox}">
        </ComboBox>

        <Border BorderThickness="1"  BorderBrush="Red"  CornerRadius="5"  MinWidth="200" MaxWidth="300" HorizontalAlignment="Center" Margin="0 20 0 0">
            <Grid Margin="5">
                <TextBlock x:Name="textSearch" MouseDown="textSearch_MouseDown" Text="Пусто" Style="{StaticResource textHint}" FontSize="20"/>
                <TextBox x:Name="search"  Foreground="Crimson" TextChanged="search_TextChanged"  CaretBrush="Black"  Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"  Background="Transparent"  Style="{StaticResource textBox}" FontSize="20" IsReadOnly="True"/>
            </Grid>
        </Border>

        <Border BorderThickness="1"  BorderBrush="Red"  CornerRadius="5"  MinWidth="200" MaxWidth="300" MaxHeight="150" HorizontalAlignment="Center" Margin="0 20 0 0">
            <Grid Margin="5">
                <TextBlock x:Name="textDesc" MouseDown="textDesc_MouseDown" Text="Пусто" Style="{StaticResource textHint}" FontSize="20" TextWrapping="Wrap"/>
                <TextBox x:Name="desc"  Foreground="Crimson" TextChanged="desc_TextChanged"  CaretBrush="Black"  Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"  Background="Transparent"  Style="{StaticResource textBox}" FontSize="20" TextWrapping="Wrap" IsReadOnly="True"/>
            </Grid>
        </Border>

        <Button Content="Удалить" Margin="0 50 0 0" HorizontalAlignment="Center" FontSize="26" Height="45" Width="160" Style="{StaticResource backBttn}"  Cursor="Hand" Command="{Binding DeleteCommand}"/>
    </StackPanel>
</UserControl>
